<?php
// +----------------------------------------------------------------------
// | saiadmin [ saiadmin快速开发框架 ]
// +----------------------------------------------------------------------
// | Author: your name
// +----------------------------------------------------------------------
use Webman\Route;

#foreach ($column in $tables)
#if[$package_name != ""]
// 注册${menu_name}路由
fastRoute('/${namespace}/${package_name}/${business_name}',\${namespace_start}controller${namespace_end}\${class_name}Controller::class);
#/if
#if[$package_name == ""]
// 注册${menu_name}路由
fastRoute('/${namespace}/${business_name}',\${namespace_start}controller${namespace_end}\${class_name}Controller::class);
#/if
#/foreach
